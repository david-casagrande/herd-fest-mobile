import container from './components/container';
import list from './components/list';
import setTimeRow from './components/set-time-row';

const dayListStyles = {};

export default Object.assign(dayListStyles, container, list, setTimeRow);

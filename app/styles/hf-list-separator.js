import colors from './_colors';
import { StyleSheet } from 'react-native';

const separator = {
  backgroundColor: colors.lightGrey,
  height: StyleSheet.hairlineWidth
};

export default StyleSheet.create({ separator });

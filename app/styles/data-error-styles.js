import container from './components/container';

const dataErrorStyles = {
  message: {
    fontSize: 44,
    textAlign: 'center'
  }
};

export default Object.assign({}, container, dataErrorStyles);

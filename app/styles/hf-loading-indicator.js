import { StyleSheet } from 'react-native';

const view = {
  flex: 1,
  justifyContent: 'center',
  alignItems: 'center'
};

const text = {
  fontSize: 64
};

export default StyleSheet.create({ text, view });

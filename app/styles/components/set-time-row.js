const setTime = {
  backgroundColor: 'red',
  width: 74,
  color: 'white',
  textAlign: 'center'
};

const content = {
  flex: 1,
  paddingLeft: 10
};

const toggleSetTime = {
  backgroundColor: '#eee',
  width: 40,
  textAlign: 'center'
};

export default { setTime, content, toggleSetTime };
